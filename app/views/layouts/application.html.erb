<!DOCTYPE html>
<html>
  <head>
    <title>Quick Question - Decide Socially</title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag 'jquery', 'application' %>
    <!-- This javascript is breaking the delete function -->
    <%#= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jquery/1.4.2/jquery.min.js" %>
  <%= javascript_include_tag "http://ajax.googleapis.com/ajax/libs/jqueryui/1.8.1/jquery-ui.min.js", "http://www.google.com/jsapi" %>
  <%= javascript_include_tag 'rails' %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
 
  <script type="text/javascript">
    // Load the Visualization API and the piechart package.
    google.load('visualization', '1', {'packages':['corechart']});

    // Set a callback to run when the Google Visualization API is loaded.
    google.setOnLoadCallback(drawChart);
  
    function drawChart() {
      // Everything is loaded. Lets draw our chart...
      //alert("finished loading");
    }
    

  var _gaq = _gaq || [];
  _gaq.push(['_setAccount', 'UA-2677869-11']);
  _gaq.push(['_trackPageview']);

  (function() {
    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
  })();

  </script>
   
    
  </head>
  <body>
    <div class="header">
     Quick Question! - Decide Socially<span class="headersmall"> alpha</span>
    </div>
    <div id="container">
      <div id="user_nav">
        <div class="sign_in_with_all_div">
        <%= render 'shared/sign_in_with' %>
        </div>
        <div>
        <%= link_to 'Recent Questions', root_path() %>
        </div>
        <% if user_signed_in? %>
          <%= link_to 'Your Questions', projects_path() %><br />
        <% end %>
      </div>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <%= content_tag :h1, yield(:title) if show_title? %>
      <%= yield %>
    </div>
    
    <div class="footer">
     Provided By: <%= link_to 'Benerino.com', 'http://www.benerino.com' %>
    </div>
  </body>
</html>
